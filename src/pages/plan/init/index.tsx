import DestinationInput from "@/features/plan/init/components/DestinationInput";

const Init = () => {
  return (
    <div className='w-full flex flex-col justify-center gap-6 items-center mt-10 px-4'>
      <DestinationInput />
    </div>
  );
};

export default Init;
